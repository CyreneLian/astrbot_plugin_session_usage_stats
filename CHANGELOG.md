# 📊 astrbot_plugin_session_usage_stats CHANGELOG

## 🌸 v2.1.6 (2026-08-23)

- **配置面板文案优化**：优化了部分设置名称和说明，更易理解配置作用。
- **权限体系优化**：删除子命令内部的权限硬校验，权限统一跟随指令的框架权限。

## 🌸 v2.1.5 (2026-08-07)


- **补全插件行为描述**：为所有指令（会话统计、会话统计模式、会话统计诊断、会话统计全部）、事件监听器（capture_user_message）和钩子（capture_bot_response）补充了完整的文档字符串描述。

## 🌸 v2.1.3 (2026-08-05)

- **配置默认值更新**：更新了默认配置 models/config.py、配置 Schema _conf_schema.json。
- **文档说明微调**：修正并补齐了 README 配置说明表格中的默认值描述。

## 🌸 v2.1.2 (2026-08-03)

- 美化了插件文档、增加了几个未知的统计bug并和昔涟聊了一会天。

## 🌸 v2.1.1 (2026-08-02)

> **版本定位**：全时间跨度追溯、高性能查询重构与双层分级存储清理大版本更新。

### ✨ 1. 全时间跨度扩展
- **新增时间范围支持**：在面板与后端全套补齐了 **「过去 1 年」**（`year`）与 **「全部历史」**（`all`）两大时间跨度选项。
- **趋势图动态月份聚合**：针对长跨度视图，趋势概览折线图自动切换为按自然月（`YYYY-MM`）大粒度轨迹绘图，支持直观查看跨年/全历史的每月 Token 演变。

### ⚡ 2. 数据库与高性能查询优化
- **SQL 范围查询重构**：将原先按时间推演生成数千至数十万 SQL 占位符的 `WHERE bucket_key IN (?, ?, ...)` 慢查询，优化为极速的范围过滤 `WHERE bucket_key >= min_key AND bucket_key <= max_key`。彻底解决了长跨度视图下因占位符超过 SQLite 限制导致的查询异常与数据偏小问题。
- **API 路由白名单放行**：修复了后端 `core/api.py` 在校验 `bucket_type` 时拦截 `year` 和 `all` 并强制重置为 `day` 的隐蔽问题。

### 🛡️ 3. 双层分级自动数据清理机制
- **第一层（小时桶精简）**：超过 30 天的小时桶（`hour`）细粒度数据为了精简数据库大小，固定自动擦除，释放 73%+ 的冗余存储空间，保持数据库毫秒级极速响应。
- **第二层（全量总账保留）**：超过全量保留天数（默认 **365 天 / 1 年**）后，彻底清除日/周/月总账桶数据；可灵活通过 `auto_cleanup_enabled` 随时开启或停用。

### 🎨 4. UI 视觉与配置文档升级
- **时间按钮尺寸统一**：为顶部 5 个范围切换按钮设置了固定 100px 最小宽度与居中对齐，解决字数不一导致的按钮大小参差问题。
- **配置与 Schema 全量同步**：更新了 `_conf_schema.json`、`metadata.yaml` 与 `README.md` 中的默认值（365 天）、提示 Hint 及说明文案。
