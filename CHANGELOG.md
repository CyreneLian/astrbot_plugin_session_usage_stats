# 📊 astrbot_plugin_session_usage_stats CHANGELOG

## 🌸 v2.2.0 (2026-08-26)

> **版本定位**：模型统计数据源全插件库化与新增用量告警体系的重磅升级大版本。

### ⚠️ 重要升级提示

由于统计层与查询层的大幅重构，为避免旧架构的历史数据污染新的统计数据导致统计结果出现大幅偏差，建议从 2.1.6 及之前版本升级到此版本的老用户前往面板手动清空数据后再使用！

### ✨ 统计层重构
- **对话模型改为从主库 provider_stats 增量同步**：新增 `sync_model_usage_from_provider_stats`，每次自动扫描后按游标增量同步主库对话模型调用记录到插件 `model_usage_stats` 表；事件捕获不再双写模型表（只写会话统计），对话模型数据源唯一化，不再依赖事件捕获的完整性。
- **数据层精确扣减合并**：同步对话模型时，在数据层调用新增的 `deduct_model_call_row` 按模型与时间桶精确扣减重复的底层调用记录，`model_call_stats` 仅保留纯后台差额（嵌入、重排、插件直连等）。
- **记录时区修复**：模型调用记录时间由无时区的本地 `now()` 改为明确指定 Asia/Shanghai 时区，避免被规范化逻辑误判为 UTC 导致分桶偏移 8 小时（跨天错桶）。

### 🔍 查询层重构
- **模型用量查询彻底摆脱主库实时依赖**：`_query_model_usage` 不再实时查询主库 `provider_stats`，全部改读插件自身数据库，函数体积近乎减半；清理主库历史不再影响任何视图。
- **移除查询时内存扣减（covered）**：老版查询「全部模型」时需在内存中动态扣减对话基线与后台记录的重叠部分；现由统计层数据预扣减取代，查询直接合并结果，逻辑更简单、口径更一致。
- **智能分桶查询**：24小时 / 7天视图使用小时桶（hour）保证小时级精度；30天 / 1年 / 全部视图切换日桶（day），避免长跨度视图受小时桶 30 天固定清理的影响导致数据缺失。
- **趋势查询彻底移除主库兜底**：删除 `_iter_history_bot_rows` 主历史库逐行兜底读取路径（含 api.py 趋势接口的调用），趋势图数据完全来自插件自身聚合桶。

### 新增用量告警体系
- **每日用量告警全新上线**：支持全局 / 单会话双阈值，超限自动向指定目标发送合并告警消息；QQ 号私聊直填、群号加 g: 前缀走群聊，支持多目标。
- **冷却记录持久化**：告警冷却记录保存到数据库（scan_state 表），插件重载 / 重启后自动恢复，同类型同天不会重复发送；跨天自动重置，新的一天重新计算。
- **发送失败不标记冷却**：仅当至少一个目标接收成功才写入冷却记录；全部失败时下次检查将携带最新实时数据重新合并发送，告警不丢失。

### 🧹 清理机制升级

- **自动扫描后联动清理**：找回「每次自动扫描后执行自动清理」触发点（早期模块化拆分时遗失），配合原有 1 小时冷却防抖，自动清理稳定每小时最多执行一次。
- **VACUUM 行为严格区分**：移除隐藏配置项 `auto_cleanup_vacuum`，自动清理只删除旧数据、不再重组数据库文件；仅面板「清空数据」手动物理清空时执行 VACUUM 瘦身。
- **「清空数据」升级为真物理清除**：由旧版"仅清理 30 天前旧数据"升级为 DELETE 全部统计表 + VACUUM 数据库文件瘦身 + 扫描游标重置，彻底解决清空后旧数据复活或残留的问题。

### 🖥️ 面板交互修复
- **图表弹出层可交互**：修复模型占比弹出层 `pointer-events` 失效导致无法复制 / 选中文本的问题，并支持鼠标悬停弹出层时不自动消失。
- **清空按钮状态提示优化**：二次确认期间不再误改页面状态栏文案，避免「请再次点击确认清空」残留误导。

### ⚙️配置更新
- **配置面板分组化重组**：配置项由平铺式重组为「用量统计设置」「用量告警设置」两大分组，结构更清晰。
- **强制自动扫描与事件钩子开启**：移除「启用自动扫描」「启用事件钩子捕获」两个开关，两项能力强制开启以保证统计准确性。
- **配置非法值兜底**：新增 `_safe_int` 安全解析，全部数值型配置填入字母、小数、负数等非法值时自动回退默认值（间隔/上限/天数附下限保护，不低于 1）；平台列表字段误填单个字符串时自动包装为列表，不再被拆成单字符导致统计异常。






## 🌸 v2.1.6 (2026-08-23)

- **配置面板文案优化**：优化了部分设置名称和说明，更易理解配置作用。
- **权限体系优化**：删除子命令内部的权限硬校验，权限统一跟随指令的框架权限。

## 🌸 v2.1.5 (2026-08-07)


- **补全插件行为描述**：为所有指令（会话统计、会话统计模式、会话统计诊断、会话统计全部）、事件监听器（capture_user_message）和钩子（capture_bot_response）补充了完整的文档字符串描述。

## 🌸 v2.1.3 (2026-08-05)

- **配置默认值更新**：更新了默认配置 models/config.py、配置 Schema _conf_schema.json。
- **文档说明微调**：修正并补齐了 README 配置说明表格中的默认值描述。

## 🌸 v2.1.2 (2026-08-03)

- 美化了插件文档、增加了几个未知的统计bug并和昔涟聊了一会天。

## 🌸 v2.1.1 (2026-08-02)

> **版本定位**：全时间跨度追溯、高性能查询重构与双层分级存储清理大版本更新。

### ✨ 1. 全时间跨度扩展
- **新增时间范围支持**：在面板与后端全套补齐了 **「过去 1 年」**（`year`）与 **「全部历史」**（`all`）两大时间跨度选项。
- **趋势图动态月份聚合**：针对长跨度视图，趋势概览折线图自动切换为按自然月（`YYYY-MM`）大粒度轨迹绘图，支持直观查看跨年/全历史的每月 Token 演变。

### ⚡ 2. 数据库与高性能查询优化
- **SQL 范围查询重构**：将原先按时间推演生成数千至数十万 SQL 占位符的 `WHERE bucket_key IN (?, ?, ...)` 慢查询，优化为极速的范围过滤 `WHERE bucket_key >= min_key AND bucket_key <= max_key`。彻底解决了长跨度视图下因占位符超过 SQLite 限制导致的查询异常与数据偏小问题。
- **API 路由白名单放行**：修复了后端 `core/api.py` 在校验 `bucket_type` 时拦截 `year` 和 `all` 并强制重置为 `day` 的隐蔽问题。

### 🛡️ 3. 双层分级自动数据清理机制
- **第一层（小时桶精简）**：超过 30 天的小时桶（`hour`）细粒度数据为了精简数据库大小，固定自动擦除，释放 73%+ 的冗余存储空间，保持数据库毫秒级极速响应。
- **第二层（全量总账保留）**：超过全量保留天数（默认 **365 天 / 1 年**）后，彻底清除日/周/月总账桶数据；可灵活通过 `auto_cleanup_enabled` 随时开启或停用。

### 🎨 4. UI 视觉与配置文档升级
- **时间按钮尺寸统一**：为顶部 5 个范围切换按钮设置了固定 100px 最小宽度与居中对齐，解决字数不一导致的按钮大小参差问题。
- **配置与 Schema 全量同步**：更新了 `_conf_schema.json`、`metadata.yaml` 与 `README.md` 中的默认值（365 天）、提示 Hint 及说明文案。
